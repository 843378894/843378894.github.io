---
title: 算法-链表-合并俩个有序列表.
date: 2023-05-17 19:30:00
categories: 链表
tags: 算法
---

### 算法-链表-合并俩个有序列表

#### 题

将两个升序链表合并为一个新的 升序 链表并返回。新链表是通过拼接给定的两个链表的所有节点组成的。

输入：l1 = [1,2,4], l2 = [1,3,4]
输出：[1,1,2,3,4,4]

#### 解

```
   /**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} list1
 * @param {ListNode} list2
 * @return {ListNode}
 */
 /**
 *  1--》2--》4
 *  1--》3--》4
 *对为对比谁小谁在前边
 *  1
 *  ^
 *  |
 *  1
 *  再拿大的和下一个节点的比因为是升序的如果L1的比L2的下一个及节点下那么久要对比一下L1的下个节点谁
 *  1
 *  ^
 *  |
 *  1
 * */
var mergeTwoLists = function(list1, list2) {
    // console.log(list1.val, list2.val)
    if(list1 === null){
        return list2
    }
    //  当链表指向没有了之后指向另外一个链表的节点就此结束
     if(list2 === null){
        return list1
    }
    // 如果L1的值小于L2就将小的排在前面大的放在后面
    if(list1.val < list2.val){
        // 链表指针指向下一个链表 回调方法是将小的下一位与当前大的对比再次指向小
        list1.next = mergeTwoLists(list1.next,list2)
        // 谁是头就返回那个链表
        return list1
    }else{
        list2.next = mergeTwoLists(list1,list2.next)
        return list2
    }

};
```
